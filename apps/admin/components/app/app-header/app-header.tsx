import { Surface } from '@/components/ui';

export function AppHeader() {
  return (
    <Surface className="flex h-16 items-center px-6">
      <h1 className="text-lg font-semibold">Dashboard</h1>
    </Surface>
  );
}
